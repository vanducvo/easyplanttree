<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify</title>
    <%- include ('../partials/require') %>
    <style>
        html,
        body {
            width: 100%;
            height: 100%;
            background-color: #fafbfc;
        }

        .image-brand {
            max-width: 20%;
            max-height: 20%;
            opacity: 0.9;
        }

        .alert{
            margin-top: 1rem;
            text-align: center;
        }
    </style>
</head>

<body>
    <header>
        <div class="container-fluid d-flex justify-content-center">
            <img class="image-brand" src="/public/assets/images/logo.png" alt="Eassy Plant Tree">
        </div>
    </header>
    <div class="container-fuild d-flex justify-content-center form">
        <div class="d-flex flex-column col-sm-6 col-md-5 col-lg-4 verify">
            <div class="alert <%= verify ?'alert-success': 'alert-danger' %>" role="alert">
                <b><%= verify ? 'Verify success!' : 'Vertify Fails! Please Sigup Again' %></b>
            </div>
            <% if(verify){ %>
                <a href="/auth/login">
                    <button type="button" class="btn btn-success container-fluid">Login</button>
                </a>
            <% } else {%>
                <a href="/account/signup">
                    <button type="button" class="btn btn-info container-fluid">Expired sign up! Please Sign up Again!</button>
                </a>
            <% } %>
        </div>
    </div>
</body>
